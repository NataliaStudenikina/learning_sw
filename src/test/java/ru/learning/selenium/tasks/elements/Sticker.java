package ru.learning.selenium.tasks.elements;

import org.openqa.selenium.WebElement;

public class Sticker {
    private WebElement element;

    public Sticker(WebElement element) {
        this.element = element;
    }
}
